const Loading = () => {
    return (
        <div className="w-screen h-screen z-50 bg-albescent-white flex items-center justify-center fixed top-0 left-0">
            Loading
        </div>
    );
};

export default Loading;
